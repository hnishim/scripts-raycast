# Raycast scripts

Raycast commands and their local helper scripts are managed from this directory.
Business-day calculation is part of this repository at `calcBusinessDays`.

Runtime environments, credentials, generated files, and logs are excluded.
